# Claim inventory

The load-bearing claims the README makes, each with its evidence and where CI exercises it. When a claim's evidence
moves or its version pin changes, this table is the checklist. Last audited: 2026-07-24, against telescope `1.3.0`,
MapStruct `1.6.3`, Spring Boot `4.1.0`, Quarkus `3.37.3`.

| Claim                                                                                                                                  | Evidence                                                                                                                                          | CI                                                      |
| -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| Runtime mapper works under GraalVM native-image                                                                                        | 8-capability `NativeVerify` binary ([contract](native-image.md))                                                                                  | `native-image.yaml` — substrate pushes to main + weekly |
| Codegen lands in MapStruct's performance class (~1.07× flat, ~1.15× deep near-tie)                                                     | [perf-mapstruct-comparison.md](perf-mapstruct-comparison.md), two CI runs, MapStruct 1.6.3                                                        | `benchmarks.yaml` (`workflow_dispatch`)                 |
| Runtime path within ~1.3–4× of MapStruct by shape                                                                                      | same benchmark set, runtime tier                                                                                                                  | `benchmarks.yaml`                                       |
| Same-name fields map automatically, recursion included                                                                                 | `DeepMappingTest`                                                                                                                                 | `ci.yaml`                                               |
| `backward()` reverses structural rows; `constant`/`compute` forward-only; dropped fields return null (refs) / JLS default (primitives) | `DeepMappingTest`, `NullConsistencyTest` (drop shapes)                                                                                            | `ci.yaml`                                               |
| Multi-source `merge` is forward-only (`backward` throws)                                                                               | `MergeTest.backwardThrows`                                                                                                                        | `ci.yaml`                                               |
| Strict `mapper(...)` refuses unmapped fields at construction                                                                           | `DeepMappingTest.strictModeRejectsUnmappedSource`                                                                                                 | `ci.yaml`                                               |
| MapStruct source-rename in `@Mapping(source=…)` is a compile error; unmapped target under default policy is WARN + runtime null        | [head-to-head module](../examples/mapstruct-vs-telescope/), MapStruct 1.6.3; official docs: `unmappedTargetPolicy` default WARN                   | `ci.yaml` (module tests)                                |
| MapStruct reverse = second method + `@InheritInverseConfiguration` (expressions/constants/defaults not inherited)                      | [MapStruct `@InheritInverseConfiguration` javadoc](https://mapstruct.org/documentation/stable/api/org/mapstruct/InheritInverseConfiguration.html) | — (upstream docs)                                       |
| POJO update never mutates the original; untouched subtrees are shared                                                                  | `NullConsistencyTest`, bean write-strategy tests; aliasing documented in [pojos.md](pojos.md)                                                     | `ci.yaml`                                               |
| Null containers / null `Optional` fields focus nothing on every navigation surface                                                     | `NullConsistencyTest` (9 pins)                                                                                                                    | `ci.yaml`                                               |
| The `updateAsync` executor bounds concurrent `fn` invocations, not in-flight operations                                                | `EffectsConcurrencyTest` (both directions pinned)                                                                                                 | `ci.yaml`                                               |
| `updateEither` short-circuits (later `fn` never invoked); `updateValidated` accumulates every error                                    | effect tests over `EitherK`/`ValidatedK`                                                                                                          | `ci.yaml`                                               |
| `explain()` rows: mapped / transformed / skipped / unusedSources; constants-computed slots don't appear as rows                        | `MapperExplainTest`                                                                                                                               | `ci.yaml`                                               |
| Spring registry discovers `Mapper` beans; Quarkus ditto                                                                                | starter unit tests (registries; no `@SpringBootTest`)                                                                                             | `ci.yaml`                                               |
| Multi-edit `Telescope.all` fuses provably-disjoint paths into one pass, identical results                                              | `AllOverFusionTest` (equivalence matrix + counterexample pins)                                                                                    | `ci.yaml`                                               |
| README quick-start and tour snippets compile and run                                                                                   | `ReadmeSnippetsTest`                                                                                                                              | `ci.yaml`                                               |

Rules of the table: a claim with no row does not go in the README; a "partial" in the
[coverage matrix](mapstruct-parity.md) stays labelled partial; competitor behavior cites official documentation plus the
pinned version, never only this repo's own tests.
