package io.github.eschizoid.telescope.bridgexpkg.vm;

/**
 * Field type in its own package — a @ViaMapper'd field whose type is cross-package to the bridge.
 */
public record VmAddress(String line) {}
